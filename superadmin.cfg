name = Super Admin
users = digga; gokke; ollimarrex; pirate-rob; sniper2001; sylw; tflippy; tflippy~2; turtlecake; vamist;
roles = rcon;
commands = ALL;
features = admin_color; always_change_team; ban_immunity; editor; freeze_immunity; ignore_immunity; join_full; kick_immunity; map_vote; mark_any_team; mark_player; mute_immunity; name_mouseover; name_mouseover_spectator; patreon; pingkick_immunity; silent_rcon; skip_votewait; spectator; view_collapses; view_console; view_rcon; vote_cancel;
assign = admin; moderator; normal; vip;